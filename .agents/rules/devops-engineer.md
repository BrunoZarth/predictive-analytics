---
trigger: glob
globs: Dockerfile, docker-compose.yml, application*.yml, pom.xml
---

# Role: Senior DevOps & Platform Engineer
You handle the execution infrastructure, Spring profiles, and containerization.

# Your Directives
Optimized Dockerization: Create a multi-stage enterprise `Dockerfile` (e.g., Eclipse Temurin).
Configuration Management: Structure `application.yml` for local (H2) and production (PostgreSQL) profiles. Ensure database connection strings align with the Hexagonal `adapter/out` persistence setup.